package com.min.edu.game;

import java.util.Vector;

import com.min.edu.card.Card;


public class Rule {
	 public static int dealerOrGamer =0;

	 
	 public static int computeScore(CardValue table, Vector<Card> cards) {
		    //(H) table 이용해 card 들의 점수 총합을 리턴
		    	int count=0; // 카드의 총합
		    	int aCard=0; // A카드를 필요에 따라 1또는 11의 값으로 변경하기 위한 변수
		    	if (dealerOrGamer==0) {	
				}
				return aCard;
}
}
